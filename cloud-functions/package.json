{
  "name": "presenca-mais-functions",
  "version": "1.0.0",
  "private": true,
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "lint": "echo \"Sem lint configurado\"",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "axios": "^1.4.0",
    "firebase-admin": "^12.5.0",
    "firebase-functions": "^5.0.1"
  }
}
